import express from 'express';\nimport { Income } from '../database/models.js';\n\nconst router = express.Router();\n\n// Get all income for a user\nrouter.get('/:userId', async (req, res) => {\n  try {\n    const { userId } = req.params;\n    const income = await Income.getByUser(userId);\n    res.json(income);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Get total income for a specific month\nrouter.get('/:userId/month/:month', async (req, res) => {\n  try {\n    const { userId, month } = req.params;\n    const total = await Income.getTotalByMonth(userId, month);\n    res.json({ total });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Add new income\nrouter.post('/:userId', async (req, res) => {\n  try {\n    const { userId } = req.params;\n    const { source, amount, month, description } = req.body;\n    const income = await Income.create(userId, source, amount, month, description);\n    res.status(201).json(income);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nexport default router;\n